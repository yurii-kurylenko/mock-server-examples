version: "4.2"

compose:
  files:
    - docker-compose.yml

interaction:
  simple-js-server:
    service: simple-js-server
    subcommands:
      bash:
        command: bash
      npm:
        command: npm
      start:
        command: npm run start
    compose:
      run_options: [service-ports, use-aliases]
